// Template voor secrets.h — kopieer dit bestand naar secrets.h en vul je
// eigen WiFi-gegevens in. secrets.h zelf staat in .gitignore.
#pragma once

#define WIFI_SSID      "JOUW_SSID"
#define WIFI_PASSWORD  "JOUW_WACHTWOORD"
